// Import the JS objects directly
import ruleSchema from "./schemas/rule.js";
import ruleGroupSchema from "./schemas/ruleGroup.js";

export const APP_CONSTANTS = {
  APP_NAME: "MindfulBlock",
  VERSION: "1.0.0",
};

// Re-export them
export { ruleSchema, ruleGroupSchema };
